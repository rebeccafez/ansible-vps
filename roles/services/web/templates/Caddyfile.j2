www.{{ domain_name }} {
  root * {{ webroot }} 
  file_server
  encode gzip zstd
  tls "{{ alerts_email }}" 
}

{{ domain_name }}:80 {
  redir https://www.{{ domain_name }}{uri} permanent 
}

{{ domain_name }}:443 {
  redir https://www.{{ domain_name }}{uri} permanent 
  tls "{{ alerts_email }}" 
}
